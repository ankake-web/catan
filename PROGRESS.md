# PROGRESS — カタン航海者版（最終更新: 2026-07-03）

> Vite + TypeScript のブラウザ版カタン。リポジトリ: `ankake-web/catan`（origin）。
> 本番: https://ankake-web.github.io/catan/ （`.github/workflows/deploy.yml` が main への push で自動デプロイ）。
> 戦国リスキン版「**100万石**」は同一 git の別 worktree `…/game/100man-goku`（ブランチ `art-pass/100man-goku`）。
> 100万石の本番: https://ankake-web.github.io/100man-goku/ ＝ **hyaku リモート**（`ankake-web/100man-goku`）の main へ push。
> **作業ルール（厳守）**: 変更は必ず `tsc`/`vitest`/`build` 緑を保つ。フィーチャーブランチ＋PR で進める。
> **main への直接 push／PR の main マージ／hyaku/main への push（いずれも本番デプロイ）は毎回ユーザの明示許可後**。
> 回答は日本語。完了/入力待ちごとに CLAUDE.md の beep+トースト通知コマンドを実行する。

## 1. ゴール
カタン**航海者版（Seafarers）を公式準拠**で完成させ、本番で安定稼働させること。
- 公式8シナリオ（S1〜S8）＋New World＋公式アプリ由来の追加盤（干ばつ/宝島/オセアニア/大カタン/オアシス）
  ＋C&K×航海者コンボ3本を実装済み。表示シナリオは**14本**（基本/都市と騎士/航海者7/C&K複合3/オアシス）。
- 正の仕様書: `docs/カタン航海者版_ルール仕様メモ.md` / `docs/航海者版_公式準拠リビルド計画.md` /
  `docs/ClaudeCode_カタン全ルール監査プロンプト.md`。公式アプリのスクショは `photo/`（gitignore）。
- 派生ゴール: 100万石フォークへの順方向同期（手順・ログは **SYNC.md** が唯一の正）。

## 2. 完了したこと（新しい順・すべて main マージ＝本番反映済み）
- **2026-07-02/03 見直しセッション**: マージ済みローカルブランチ7本削除＋remote prune／PROGRESS・SYNC を
  最新化（PR#12）／SYNC.md §5 を catan・フォークの両 worktree で同一内容に統一／メモリ索引更新。
  フォークへも docs push 済み。※PR#12 デプロイが GitHub Pages の一時障害で1回失敗→ `gh run rerun --failed` で成功。
- **2026-07-02 PR#11 `fix/seafarers-audit-followups`**: ultracode 多エージェント再監査（26体・確定5/提起6）の
  追随バグ5件を修正（vitest 948緑）:
  ①本島↔霧境界の赤6/8隣接（オアシス約4割）→ `randomizeHomeAndFog` で本島＋霧を協調生成リトライ
  ②SETUP中の霧公開で資源只取り＋最後の初期開拓地の二重取得 → `revealFogAround` に `grantReward` を追加し SETUP は抑制
  ③海賊コマが霧タイル（type=sea だが探索で陸化）に初期配置 → 候補フィルタに `!t.fog`
  ④`downgradeCity` が開拓地コマ在庫0で「幻の開拓地」を生成 → 在庫0なら都市を撤去（公式CK）
  ⑤CK×航海者で水道橋（科学Lv3）が金タイル産出者に誤発火し二重付与 → `computeGoldPicks` を見て対象外に。
  同日 **実機検証（Playwright）**: 全14シナリオ盤が実描画エラー0・SETUP有効頂点表示OK（大盤の初回描画≈2.5秒は正常）。
- **2026-07-02 フォーク同期に着手**: 100万石へテーマ非依存バグ2件を移植（船移動の海賊封鎖ガード／
  開放端判定を「自分の船のみ」に是正）＋テスト2件。fork vitest 738緑。hyaku/main へ push＝100万石本番反映。
- **2026-07-01 PR#10 `fix/seafarers-pr9-bugs`**: PR#9 の敵対的多角レビュー（31体）で確定した12件を修正（vitest 939緑）:
  オアシス霧の資源地が産出しない／CK金タイル死にメカ（`applyGoldChoicePhase` 共通化）／大カタン都市8
  （`remainingCities=maxCities`）／霧の数字制約／SETUP財宝只取りガード／船UI誤表示（`noShips` を GameState 配線）／
  地域ボーナス phase 非依存／財宝バンク減算／`mainIslandTileIds` タイブレーク／フォールバック防御／テスト穴埋め。
- **2026-06-30 PR#7→#8→#9**: 盤の毎ゲームランダム化（骨格固定・配置シャッフル・赤6/8制約）→ 島ごと固定構成＋
  盗賊/海賊の公式化＋初期配置で道/船選択 → **写真準拠リビルド**（4つの島51hex/霧の島/オセアニア/砂漠を越えて/
  大カタン）＋新メカ2種（**抜けている数値トークン** `pendingNumber`/`numberTokenSupply`・**地域ボーナス**
  `bonusRegionTiles`/`regionBonus`）＋C&K×航海者コンボ3本＋**オアシス**（基本＋道で霧探索・財宝・`noShips`）。
- **〜2026-06-29**: 公式準拠監査＋修正（PR#3）・S7軍船/艦隊戦・S6織物・初心者支援（ツールチップ/初心者モード/
  遊び方ヘルプ）・シナリオ別ルール表示。詳細は git log と本ファイルの過去版（`git log -- PROGRESS.md`）。

## 3. 決定事項とその理由
- **盤 footprint は共通37/51ヘックスのまま**（公式35の厳密化は座標作り込みが必要＝実物盤の高解像度写真待ち）。
  `photo/` の公式アプリ・プレビュー画像は陸/海/霧の塊のみで資源・数字・港が写らず座標詰めには不可と確定（2026-06-29）。
- **公式データが無い項目は触らない**（捏造回避）: S8不思議の正式要件・S3霧の正確な数字/金の枚数など。
- **S7 は軍船・艦隊戦を実装**（オプションA・ユーザ選択）。軍船化の対象選択UIと戦闘報酬「任意1枚」UIは
  見送り＝自動選択が妥当（対象選択は結果に無影響・手番外プレイヤーも勝ちうるため対話化が構造的に厄介）。
- **設計通りでバグではない**と確定（PR#10時）: オアシス財宝の初期到達不能（探索で霧が陸化し到達可能になる）／
  newshores の小島資源は「小島1プール」（本島↔小島は非混合・per-island 固定は仕様外）。
- **SETUP は無償報酬なし**が方針: 財宝（collectEdgeToken）も霧公開の資源（revealFogAround）も SETUP 中は
  付与しない（PR#10→PR#11 で段階的に統一）。
- **フォーク大移植（航海者リビルド一式）は保留**（2026-07-02 ユーザ判断）: フォークは旧航海者エンジンのまま
  （engine 5本欠落＝explore/cloth/wonders/pirateIslands/seaTokens）。移植は XL/高リスクで、**七不思議/織物/財宝/
  要塞/艦隊/地域ボーナスの戦国名を `…/100man-goku/docs/reskin/GLOSSARY.md` で確定するのが前提**。
- **cherry-pick は使わない**（リスキン文字列の巻き戻し防止）。同期は SYNC.md §3 の手順で手作業。

## 4. 未完了タスク（優先順位順・次セッションはここから）
1. ~~**フォーク（100万石）の続き**~~ **✅ 2026-07-03 完了・本番反映済み**（SYNC.md §5 が正）:
   - 小粒3件（①islandBonus のプレイヤー別配列化 ②普請カードで船も無料配置 ③資源アニメ stagger 上限＋
     ウォッチドッグ固着解除）＝移植・検証・hyaku/main デプロイ済み（fork `7983b5f`/`a312058`/`261fbba`）。
   - 大物＝**航海者フルリビルド全移植**（engine5本＋scenarios刷新＋types/配線/テスト13本）を戦国テーマ
     （航海者→水軍・七不思議→七名城・織物→絹・砂漠→荒野）で完遂。vitest **915**緑・実機ブラウザ
     全22シナリオ描画エラー0・hyaku/main デプロイ済み（fork squash `cf492f5`）。旧シナリオID整合も
     catan 準拠で解決（greatercatan=大いなる国＝数値トークン盤に意味変更）。**フォークは catan `fdb88e7`
     時点に追随済み**。残（任意・低優先）: コメント/テストdescriptionの catan 用語の戦国reskin（dev向け）。
2. **公式地図画像（実物盤の高解像度写真）が手に入ったら**: タイル座標・港位置/種別のピクセル単位の
   合わせ込み（[D1]港・[N4]厳密35化）。
3. **公式データ入手後の積み残し**: S8七不思議の正式要件／S3霧の島の正確な数字・金の枚数。
4. **実機目視（人間）**: 新盤の実プレイ体感（オアシス/大カタン/CKコンボ）・S7軍船・S6織物・モバイル操作。
   自動検証（起動・描画・エラー0・SETUP頂点）は 2026-07-02 に全14盤で確認済み。残るは操作感のみ。

## 5. 詰まっている点・注意点・保留中の判断
- **デプロイ権限**: main への直接 push／PR マージ／hyaku/main への push は**毎回ユーザの明示許可**。勝手にやらない。
- **GitHub Pages の一時障害**: `deploy-pages` が「Deployment failed, try again later」で落ちることがある
  （2026-07-02 実績）。ビルド起因でなければ `gh run rerun <id> --failed` で回復する。
- **vitest が稀に ESM ローダで一時クラッシュ（exit 134）**: テスト失敗ではない。再実行すれば緑。
- **盤のタイル種別/数字を変えると決定論スモークの seed 軌道が変わる**: 該当スモークは複数 seed で頑健化済みだが、
  盤を触ったら全テスト＋スモークを再確認。**BIG_MAIN_ISLAND は S4/S5/S6/S7/NW/fog で共有**＝変更は全部に波及。
- **ローカル main は作業前に `git pull`**（マージコミット分 origin より遅れがち）。
- **`photo/` のフォルダ名の罠**: `photo/大カタン`＝C&K大カタン(20点)、`photo/騎士と都市　大カタン`＝航海者大カタン(18点)。
- **フォーク側の検証**: 100万石 worktree で `npm run typecheck`＋`npx vitest run`（738緑）＋`npm run build`。
  用語は必ず `docs/reskin/GLOSSARY.md`（フォーク側）を正典に変換する。
- 参考: バグ対応の全履歴と経緯はメモリ `seafarers-pr9-known-bugs` / `seafarers-official-rebuild`、
  フォーク同期の現状は SYNC.md §5（両 worktree で同一内容に統一済み）。
- 検証コマンド: `npm run typecheck`（src）/ `npx vitest run`（**948緑**・2026-07-03）/ `npm run build`。
  dev は `npm run dev`、LAN サーバは `npm start`。

---

## 過去ログ（要約。詳細は git 履歴と各PR本文）
- 2026-06-30〜07-02: 航海者リビルド（PR#7/#8/#9）→ 敵対レビュー修正（PR#10）→ ultracode 監査修正（PR#11）
  → フォーク同期着手 → docs 整理（PR#12）。※本文 §2 参照。
- 2026-06-28〜29: 公式準拠リビルド（S1〜S8＋NW・37hex・PR#3）／S7軍船・艦隊戦／S6織物強奪／初心者支援（PR#4〜）。
- 2026-06-12〜13: 航海者拡張の初期実装・多エージェント監査・マージ前レビュー・拡張プラン発展項目（A/B/C系）。
