# PROGRESS — カタン航海者版（最終更新: 2026-06-29）

> このリポジトリは Vite + TypeScript のブラウザ版カタン。本番: https://ankake-web.github.io/catan/ 。
> fork: `ankake-web/catan`（origin）。戦国リスキン版「100万石」は同一リポジトリの worktree（同期は SYNC.md 参照）。
> **作業ルール（厳守）**: 変更は必ず `tsc`/`vitest`/`build` 緑を保つ。フィーチャーブランチ＋PRで進める。
> **main への直接push／PRの main マージ（＝本番デプロイ）はユーザの明示許可後**のみ。回答は日本語。
> 完了/入力待ちごとに通知（CLAUDE.md のbeep+トースト コマンド）を出す。

## 1. ゴール
カタン**航海者版（Seafarers）を公式ルールブック準拠**にする。公式8シナリオ（S1〜S8）＋New World は
実装済み。本テーマの主眼は「**公式との乖離（盤面コンポ枚数・島構造・固有ルール）を監査して潰す**」こと。
正の仕様: `docs/カタン航海者版_ルール仕様メモ.md` / `docs/航海者版_公式準拠リビルド計画.md` /
`docs/ClaudeCode_カタン全ルール監査プロンプト.md`（2026-06-29 にリポジトリへ取り込み・docs/ に集約）。

## 2. 現在の状態（最重要）
- **PR #3 は main へマージ済み＝本番デプロイ完了**（2026-06-29・ユーザ許可後）。航海者版の監査・公式準拠化が
  https://ankake-web.github.io/catan/ に反映。GitHub Actions `deploy.yml` が main push で自動デプロイ（success）。
- **新規: 初心者支援機能を `beginner-mode` ブランチ＋PR #4 で実装**（ホバー説明＋初心者モード）。**未マージ・デプロイ可否は確認待ち**。
- **テスト 858 緑** / `tsc --noEmit` 緑 / `npm run build` 緑（最終確認 2026-06-29）。
- 旧ブランチ `seafarers-audit-and-fixes` はマージ済み。現在の作業ブランチは `beginner-mode`（main 先行）。

### 2.0 2026-06-29 後半（初心者支援・PR #4）
- **ヘックスのホバー説明（常時ON）**: 地形・産出資源・出やすさをツールチップ表示（`src/renderer/hexTooltip.ts`）。
- **初心者モード（ホームで切替・localStorage）**: 局面ガイド（助言バナー）＋初期配置のおすすめ⭐
  （`src/renderer/beginnerHints.ts`）。実ブラウザ(Playwright)で表示・OFF時非表示・エラー0を確認。
- 次の一手候補（任意）: 用語の?ヘルプ、交易の損得アドバイス、タッチ端末向けの「マスを長押しで説明」。

### 2.1 2026-06-29 追補セッション（PROGRESS §5 残タスクの消化）
- **[D6] S6 海賊移動時の織物強奪を実装**（commit e95ed82）。公式どおり「資源か織物」を無作為に1枚奪う。
  `robber.ts` に `stealResourceOrCloth`／`pirateRobbableCount` を追加し game.ts(MOVE_PIRATE)・events.ts で配線。
  織物=VP のため奪取後に勝利判定。AI は海賊を動かさず人間操作のみ。tests/cloth.test.ts +3（839→842）。
- **正の仕様md 3つを docs/ へ取り込み**（commit 89b5c75・ユーザ承認）。監査の一次資料をリポジトリに集約。
- **S7 軍船関連UI（軍船化の対象選択／戦闘勝利「任意1枚」選択）は〈両方見送り〉決定**（ユーザ判断）。
  理由: `warship` は `countWarships`（枚数）でしか使われず対象選択は見た目だけ・戦闘報酬は手番外の人も勝ちうるため
  自動選択が妥当（§4 参照）。
- **実機ブートスモーク（Playwright/Chromium）**: classic・S6・S7 が起動→盤面描画→**コンソールエラー0**を確認。
  ※インタラクティブな体感（🚢軍船化ボタン・艦隊戦・村描画）の最終確認は人間目視推奨。

## 3. このセッションで完了したこと
**フェーズA 監査**: 公式リファレンス(§4)に現コードを全項目突合。`docs/AUDIT_SEAFARERS.md`（航海者・全9シナリオ・
✅⚠️❌＋対応状況§5/§6）、`docs/AUDIT_BASE.md`（基本§4.1）、`RULES_AUDIT.md`（C&K §4.2(b)結論）を作成。
コアは概ね公式準拠・**デッドコード0**。盤面構成は9シナリオ並列＋再カウント検証のワークフローで実測。

**フェーズB 修正**（各コミットでテスト緑維持。796→839）:
| 区分 | 内容 | commit |
|---|---|---|
| バグ | [B1] 街道建設カードで船2/道1+船1 を配れない不具合 | 2e2f2aa |
| バグ | S7海賊艦隊の略奪→7の捨て札ソフトロック（新規発見） | 46ba655 |
| 固有 | [D3] S6初期配置3軒（advanceSetupをsetupRound化） | 39d0bbf |
| 基盤 | [D2][D4] 死フラグ useRobber/useLargestArmy を消費実装＋騎士ゲート | 7cfa18f |
| **S7** | [D2][D5] **S7公式化**: Ship.warship＋騎士=軍船化＋艦隊戦(軍船数 vs 海賊の目)。盗賊/最長交易路/最大騎士力OFF。複数seed完走 | (7cfa18f→74e2792) |
| 盤面 | [D7]S6村航路closed・[D8]S5港間隔・港プール調整(max7・全資源2:1) | b5caaed |
| 盤面 | **S1/S2/S4 資源ヘックス枚数を公式3人用に完全一致**・S5に金2＋トークンVP8/dev4 | d14fc66 |
| 盤面 | 全13シナリオで赤数字(6/8)隣接・金の赤数字を解消（board_red.test で恒久ガード） | 95bf882 |
| 盤面 | S6 村5→8・S7 要塞地形多様化＋推奨人数 | e161728 |
| 盤面 | [N3] New World 制約付きランダム生成（島座標固定・種別/数字をランダム化） | 25e2d6c |
| UI/固有 | S7騎士ボタン「🚢軍船化」表記・[D6]S6海賊を初回村接続まで凍結 | f63f61d |
| UI | シナリオ選択に「👥おすすめプレイヤー数」表示 | 4ad5136 |
| docs | 監査レポート/PROGRESS 更新 | 62371fe/e054ad2/ad5b97e |

→ **「資源構成・島構造・盤面ルール（赤数字等）は公式準拠」、残るは座標のピクセル一致のみ（地図画像待ち）**。

## 4. 決定事項とその理由
- **footprint は共通37のまま**（公式3人用35比で外周の海が+2）。getHexRegion は35の非対称形を作れず、
  +2は純海（フレーム）でゲーム性に無影響。陸構成は公式一致させた → 厳密35化は座標作り込み（地図画像待ち）。
- **公式データが無い項目は触らない**（捏造回避）。検証エージェントが「公式に固定値が無い＝特定値への矯正は捏造」と判断:
  [D9]S8不思議の正式要件 / [N1]S3霧の正確な数字・金の有無 / S8本島の陸種別枚数。挙動・勝利条件・質的要件は公式どおり。
- **S7はオプションA（軍船・艦隊戦の実装）を採用**（ユーザ選択）。これで盗賊/最長交易路/最大騎士力を外しても
  軍船の資源獲得が経済エンジンになり10点＋要塞制圧へ到達可能（外すだけだと到達不能でスモーク落ちを確認済み）。
- **S7要塞は4タイル定義のまま**（最大4人対応。createState が `slice(0,人数)` で割当＝3人時は3要塞・余りは小島）。
- 非公式シナリオ（群島/黄金諸島/連なる島々/大連邦）は「(非公式)」表示で据え置き。
- **S7 軍船関連UIは実装しない（2026-06-29 決定）**: ①軍船化の対象選択＝`warship` は戦力カウントにしか使われず
  どの船かは結果に無影響（見た目だけ）。②戦闘勝利「任意1枚」＝艦隊戦は ROLL_DICE 中に自動解決され手番外の
  プレイヤーも勝ちうるため対話化は構造的に厄介。自動選択（手札最少・バンク在庫あり）が妥当。回帰リスク＞便益。

## 5. 未完了タスク（優先順位順・次セッションはここから）
1. **PR #3 をレビュー → main マージ（本番デプロイ）**: ユーザの明示許可が要る。許可が出たら `gh pr merge` 等。
2. **公式地図画像が手に入ったら**: 各シナリオの厳密なタイル座標・港位置/種別をピクセル単位で合わせ込む
   （[D1]港の最終配置・[N4]枚数の厳密35化）。現状は枚数・島構造・赤数字ルールまで準拠。
3. **公式データ入手後の積み残し**: [D9]S8不思議の正式要件 / [N1]S3霧の数字・金。
4. **実機目視（人間）**: S7（🚢軍船化ボタン・艦隊戦の挙動）、S6（村8・初期3軒・織物強奪）、モバイル各種。
   ブートスモーク（起動・描画・コンソールエラー0）は 2026-06-29 に自動確認済み。残るは操作感の目視。

### 5.1 2026-06-29 追補で消化済み（§2.1 参照）
- ~~未追跡の仕様md3つをPRに含めるか相談~~ → **docs/ へ取り込み済み**（commit 89b5c75）。
- ~~S6 海賊移動時の織物強奪~~ → **実装済み**（commit e95ed82・[D6]）。
- ~~軍船化の対象船UI／戦闘勝利「任意1枚」UI~~ → **〈両方見送り〉決定**（§4・自動が妥当）。

## 6. 詰まっている点・注意点・保留中の判断
- **本番デプロイ（main マージ）は許可待ち** ＝ 5-1。勝手にマージしない。
- **vitest が稀に ESMローダで一時クラッシュ（exit 134）**することがある。テスト失敗ではなく、**再実行すれば緑**（無害）。
- 盤面のタイル種別/数字を変えると**決定論スモークの seed 軌道が変わる**ことがある（S7/ai_seafarers で経験）。
  対策として該当スモークは**複数seed**で完走検証する形に頑健化済み。盤を触ったら全テスト＋スモーク再確認。
- **BIG_MAIN_ISLAND は S4/S5/S6/S7/New World/fog で共有**。本島を変えると全部に波及するので注意（S5は専用 FORGOTTEN_TRIBE_LAND を分離済み）。
- 検証コマンド: `npx tsc --noEmit` / `npx vitest run`（842緑）/ `npm run build`。devは `npm run dev`、LANサーバは `npm start`。

---

## 過去ログ（参考・新しい順）

## 航海者 公式準拠リビルド（2026-06-28・`seafarers-official-compliance` ブランチ）

`カタン航海者版_ルール仕様メモ.md` と `航海者版_公式準拠リビルド計画.md` を正として、多エージェントの
敵対的レビュー（35件確認）を実施し、確認バグの修正と公式準拠化を段階的に実装。**公式8シナリオ
（S1〜S8）＋New World を全て実装**し、各単位で `tsc`／`vitest`（793緑）／`build` 緑を維持。

### Tier 1: 確認バグの修正
| バグ | 内容 |
|---|---|
| A2/D1 | 船移動が海賊封鎖を無視 → `canMoveShip` に移動元/先の海賊隣接ガード追加 |
| A3 | 船の開放端判定が隣接「道」を錨と誤認 → `isOpenShipEnd` を船のみに（道↔船は建物経由のみ） |
| A1 | 街道建設カードが船に非対応 → 道2/船2/道1+船1 の無料配置（エンジン＋UI） |
| C2/A4 | 新島ボーナスが島の最初の1人限定 → プレイヤー別に（`islandBonus: Record<rep, PlayerId[]>`） |
| D2 | `isShipMovable` に cheap short-circuit（描画毎の負荷軽減） |

### Tier 2: 公式準拠の土台＋シナリオ
- `ScenarioRules` 型（startingSettlements/useLongestRoute/useRobber/newIslandBonusVp/setupAnywhere）を新設し
  `createInitialGameState` で配線。`useLongestRoute=false` を最長交易路へ、プレイヤー別ホーム島
  （`playerHomeIslands`）を初期配置で記録。
- **S1 新たな海岸を目指して**: 公式名・14点・金タイル2枚。
- **S4 砂漠を越えて**: 公式版（本島＋海を渡る新天地・砂漠/金2・14点・新天地初入植+2）。
- **S3 霧の島**: 探索公開機構（霧=海扱い、船/道/開拓地で隣接マスを公開・陸なら資源1枚）。`engine/explore.ts`・12点。
- **S5 忘れられた部族**: 海辺トークン（VP/開発カード/港）を船で回収＋開拓地・盗賊は数字ヘックスのみ。`engine/seaTokens.ts`・13点。
- **S2 4つの島**: 専用37ヘックス footprint に海で隔てた4島。setupAnywhere＋プレイヤー別ホームで出発島以外への初入植+2・13点。
- **S6 カタンの織物**: 小島の村へ航路接続で織物（2枚=1点）。小島入植不可・最長交易路なし。`engine/cloth.ts`・14点/5村供給切れ。
- **S8 カタンの七不思議**: 要件クレーム＋4レベル建設（完成 or 10点＋単独最高レベル）。`engine/wonders.ts`・専用AI。
- **S7 海賊の島々**: 各自の要塞(ラホ3)を船で攻略＋海賊艦隊の略奪。制圧かつ10点で勝利。`engine/pirateIslands.ts`・専用AI。
- **New World**: setupAnywhere・出発島以外への初入植+1・12点。
- 非公式オリジナル4本（群島/黄金諸島/連なる島々/大連邦）に「(非公式)」表示。

**公式8シナリオ＋New World は全て、3人強CPUがフルゲーム完走することをスモークテストで検証済み。**
S6/S7/S8 は固有の勝利機構へCPUが向かうよう専用AI（織物=村への航路 / 不思議=要件クレーム＋建設 / 海賊=要塞攻略）を追加。

### 既知の簡略化（公式との差分・ルールブック地図があれば精緻化可能）
- 盤のタイル配置はルール準拠のデジタル版（ピクセル単位の公式配置はルールブック地図画像が必要）。
- S6 は初期配置2軒（公式は3軒）。S7 は公式の軍船による艦隊戦解決を省略し艦隊を確定的脅威として表現、盗賊/最長交易路は通常使用。

## 航海者（Seafarers）拡張の完成（2026-06-13）

Phase 0+1（盤面の土台＋船）の上に、ロードマップの主要項目をすべて実装し `main` へ反映。
各単位を「型/テスト/ビルド緑」で検証し、決定論シードの CPU 対戦で挙動を確認。詳細は
`docs/seafarers_plan.md`。**最終: 593テスト緑・`tsc` 緑・`npm run build` 緑。**

| 項目 | 内容 |
|---|---|
| Phase 3 島ボーナス | 新しい島への最初の入植に +2VP（`islands.ts`/`islandBonus`）。初期配置は本島限定に是正。 |
| Phase 5 船AI | CPU が海辺を Dijkstra で辿り新島へ拡張（`bestExpansionShip`）＋新島パイオニア加点。 |
| Phase 2 金タイル | 出目一致で任意資源を選択（新 `GOLD` フェーズ＋`CHOOSE_GOLD`）。人間モーダル/CPU自動/LAN。 |
| 2つ目の盤面 | 「群島」（本島＋新島2）。ホーム3択（基本/航海者/群島）。 |
| Phase 4 船移動 | `MOVE_SHIP`（開放端の船を1ターン1回）＋2段階操作UI。 |
| Phase 4 海賊 | `MOVE_PIRATE`（海の盗賊）。船建設封鎖＋隣接船から奪取。盗賊は陸限定に。🏴‍☠️描画。 |
| 最長交易路 | 道＋船の連続経路を算入（切替は自分の建物で）。 |
| 港 | 航海者マップの海岸線に港を自動配置。 |
| オンライン | LAN ロビーでホストがシナリオを選択（サーバ/プロトコル/ロビーUI）。 |

検証の補足: 金選択モーダルは jsdom で、海賊マーカー描画も jsdom で、LANシナリオ選択は実サーバ
経由でそれぞれ決定論テスト化。航海者/群島の3CPU対戦が両マップで GAME_OVER まで完走することを確認。
※ 2段階の船移動UI・海賊のタップ操作の体感はブラウザ目視を推奨。

## マージ前最終レビュー（2026-06-13）

第2回監査の修正一式を main へマージする直前に、4観点（エンジン正しさ/LAN同期/UIライフサイクル/オーディオ・モバイル）のレビュー＋指摘ごとに3視点の敵対的検証を実施。生7件中 **3件確認**（4件棄却）、全て修正済み。検証: `tsc --noEmit` 緑・531テスト緑・`npm run build` 緑。

| 重大度 | 修正 | 場所 |
|---|---|---|
| 中 | 再接続ハンドシェイク中（connect ペンディング）にホーム復帰→ローカル新ゲームを始めると、遅れて成立した接続が resume を送り、放棄したLANゲームが 'started' でローカルゲーム/ホーム画面を乗っ取る。`connect().then()` 冒頭に netMode ガードを追加し破棄 | `main.ts attemptReconnect` |
| 低 | `.mini-panel.mine`（後方・同特異度の `animation` 指定）が vp-gain-flash / bonus-flash を source order で打ち消し、自分の手番中＝VP獲得の主ケースでミニパネルが光らなかった。`.mine` 複合セレクタを明示（reduced-motion 側も対称に） | `style.css` |
| 低 | suspended な AudioContext に SE を積むと currentTime 凍結で同時刻に蓄積され、resume 成功時に一斉再生されバースト音になる（CPU先行手番→初タップ時）。`playSE` で running 以外は破棄 | `audio.ts` |

## 第2回 多エージェント監査と修正（2026-06-12）

6観点（公式ルール突合/エンジン境界/AI品質/ネット同期/UI状態/性能）の監査で **16件確認**（7件は敵対的検証で棄却）＋手動精読で**エンジンガード漏れ2件**を発見し、全て修正。検証: `tsc --noEmit`（src/test 両方）緑・**531テスト**（+11）緑・`npm run build` 緑・Chromium スモーク（起動→CPU対戦→ホーム復帰→ゾンビ進行なし→再開始、コンソールエラー0）。

| 重大度 | 修正 | 場所 |
|---|---|---|
| 高(チート) | `PLAY_KNIGHT` フェーズガード無し→DISCARD中の騎士で全員の捨て札を踏み倒せた。`BANK_TRADE` も PRE_ROLL/DISCARD 中に実行可能だった | `game.ts` +回帰テスト6件 |
| 高(停止) | 捨て札済みCPU（16枚→8枚残）を再選択し **LANサーバCPU駆動が恒久デッドロック** / ローカルは8秒フリーズ | `lanCpu.ts`/`main.ts`/`ai.ts` +回帰テスト |
| 高(AI) | CPUが「羊1↔鉄6」級の搾取交易を受諾。銀行レート・枚数損得ガードを追加（即建設可能になる+1枚のみ許容） | `ai.ts evaluateTradeOffer` +テスト5件 |
| 高(UI) | ホーム復帰後の旧ゲーム「ゾンビ進行」（ダイス演出finish/resize→watchdog 経由）。世代ガード+`inGame` フラグで遮断 | `main.ts` |
| 高(UI) | ローカル戦で人間がCPUの盗賊移動・盗み相手選択・初期配置を代行操作できた。`canAct` 述語+CPU手番のハイライト抑止 | `events.ts`/`main.ts` |
| 高(perf) | 毎redrawで最長交易路DFSを最大5回再計算→エンジン維持の `longestRoadLength` キャッシュ参照に | `ui.ts` |
| 中 | 街道建設カードが「盤面順で最初の辺」に配置→`bestRoadEdge` 評価に / 資源フライの遅延スポーンに世代ガード / `fxSpeed()` でLANへのCPU速度設定漏れ防止 / LAN再接続の二重スケジュール一本化+孤児クライアントclose / LAN後のCPU再戦で lanClient 残骸が state を乗っ取る問題 / ダイス演出中のLAN配信をキューイング | `ai.ts`/`main.ts` |
| 中(iOS) | AudioContext を resume せず初回SEがジェスチャ外だと全セッション無音→`resume()` + pointerdown/visibilitychange 復帰 | `audio.ts` |
| 低 | BGM ON/音量の localStorage 永続化（曲選択と対称に）/ `_bgmOscs` の onended 間引き / モバイルでVP・称号ポップが画面外→`flyTargetFor` 共用+mini-panel CSS / モーダル+/−で盤面SVG全再構築→`skipBoard` / `perMessageDeflate`（state配信35KB/通の帯域削減） | `audio.ts`/`main.ts`/`style.css`/`lanServer.ts` |

---

> 「完成一歩手前」の仕上げ＋拡張プランの残（発展）項目。**動いている機能を壊さない**ことを最優先に、小さく安全な単位で実施。
> 検証は各コミットで `npx tsc --noEmit` / `npx vitest run`（**520件 緑**）/ `npm run build`（緑）を確認済み。
> 実機ブラウザ（Chromium / Playwright）で起動・CPU対戦開始・開拓地配置・ホーム復帰→再開を通し、**コンソールエラー無し**を確認。

作業ブランチ: `finishing-touches`（`main` から分岐）。2026-06-13 に `main` へマージし `origin` へ push 済み。

---

## 0. 拡張プラン 発展項目の実装（A-5/A-6/B-5/B-6/C-2/C-4/C-7）

| コミット | 項目 | 内容 |
|---|---|---|
| (C-4) | 建設フィードバック | 開拓地/都市の設置ポップ（scale-in＋淡いグロー）、道は辺をなぞる描画アニメ。reduced-motion/instant省略。 |
| (C-7) | 称号バッジ移動 | 最長交易路/最大騎士力が移ると、バッジ(🛤/⚔)が旧→新保持者パネルへ飛ぶ（初回獲得は発光のみ）。 |
| (C-2) | 資源獲得の磨き | 資源フライ着地時に着地先パネル/ミニパネルを一瞬ポップ。※数値カウントアップは再描画モデルと衝突しリスク高のため見送り。 |
| (B-5) | ハプティクス | 配置/ダイス/交易成立/盗賊/勝利に軽い振動（**自分の操作のみ**）。対応端末のみメニューに ON/OFF（localStorage永続）。 |
| (B-6) | スクロール抑止 | `overscroll-behavior:none`（引っ張り更新/バウンス抑止、縦スクロールは維持）＋`overflow-x:hidden`。向き案内は縦/横とも良好のため付けず（ナグ回避）。 |
| (A-6) | 発展カード使用 | 中核は A-1〜A-4 で実装済み（騎士のロール前自動使用＋スマート盗賊、独占=最大不足資源、豊作=目標補完、街道建設=拡張方向）。『騎士で最大騎士力勝利』経路を end-to-end テストで固定。死蔵コードは追加せず。 |
| (A-5) | 難易度レベル | **既に実装済み**：弱/普通/強＝Easy/Normal/Hard をホームの「CPUの強さ」で選択、挙動も分岐（弱=ランダム・騎士使わない / 普通=ヒューリスティック / 強=フル＋交易優先）。強さチューニングの追加変更は回帰リスクのため行わず。 |

---

## 1. 今回直した内容（コミット単位）

| コミット | 種別 | 内容 |
|---|---|---|
| `b6cea74` | **重大バグ(高)** | **開拓地で相手の道を分断しても最長交易路が再計算されない** バグを修正。`BUILD_ROAD` にはあった `updateLongestRoad` 呼び出しが `BUILD_SETTLEMENT` に無く、分断後も旧ホルダーが +2VP を保持し勝敗判定が誤りうる状態だった。`BUILD_SETTLEMENT` に再計算を追加（`BUILD_ROAD` と同順）。**(中)** 年の豊穣/独占/街道建設が 7 の捨て札・盗賊フェーズ中でも実行できた（不正LANクライアントが捨て前に資源独占等が可能）→ 各 `PLAY_*` に MAIN/TRADE_BUILD のフェーズガードを追加。テスト追加。 |
| `2c8b059` | **重大バグ(高)** | **不正な `action` メッセージでサーバープロセス全体がクラッシュ（リモートDoS、全ルーム巻き込み）**。必須フィールド欠落の JSON（例: `response` の無い `RESPOND_TRADE`、`resources` の無い `DISCARD_RESOURCES`）が権限判定/検証で `TypeError` を投げ、ws リスナー外へ伝播して Node が落ちていた。権限判定〜検証〜`applyAction` を単一の try/catch で包み、送信者にのみエラーを返してプロセスは継続。クラッシュ回帰テスト追加。**(低)** 切断中メンバーのログ蓄積がスキップされ、再接続時に切断中のCPU代行手番がログから抜けていた → 送信のみスキップしログは常に蓄積。 |
| `b946b81` | バグ(低)/品質 | ダイス演出中に新規開始/ホーム復帰すると `diceAnimating` が残り次ゲームで入力が固まりうる → 3つのライフサイクル関数で `false` にリセット。飛行中の一時演出ノード（ダイス/資源/盗賊/略奪/得点ポップ/手番トースト）を掃除する `clearTransientFx()` を追加し残骸を残さない。 |
| `fa0842b` | アクセシビリティ | 盤面 `#board` に `role=img` + `aria-label`、共有の名前入力に `aria-label`、ルームコード入力に `aria-label`、音量スライダーに `:focus-visible` の可視フォーカス（`outline:none` で消えていた）。未使用パラメータ `setUIPhase` を `buildRobberTargetUI` から除去。 |
| `6657f54` | ドキュメント/公開前整備 | **README.md を新規作成**（概要/技術スタック/セットアップ/開発・ビルド・テスト/オンライン対戦の構成と環境変数/ディレクトリ構成）。`index.html` の `<head>` に description・theme-color・color-scheme・**絵文字SVGのインラインfavicon**（base path 非依存で GitHub Pages でも有効）・OGP・`viewport-fit=cover`（既存の `env(safe-area-*)` CSS を有効化）。 |
| `0a0b446` | 品質/クリーンアップ | 死蔵コード除去（未使用 import / 未使用関数 `deductHand`/`addToHand`/`formatResources` / 未使用ローカル / **非表示化済みで完全に死蔵だった CPU状況バナー一式**）。すべて書き込み専用・未参照で挙動不変。`noUnusedLocals`/`noUnusedParameters` の一回限りチェックが**完全クリーン**に。 |

> これらは多エージェントの読み取り専用監査（5観点 × 敵対的検証）で**確認できた 11 件**（生 17 件中）に対応。残り 6 件は検証で「実問題ではない/対応不要」と判定（誤検知）。

---

## 2. 発見したが直さなかった問題と理由

- **`getElementById('cpu-status')?.remove()` の no-op 呼び出しが4箇所残存**：CPU状況バナーを撤去したため対象要素はもう生成されない（完全に無害な防御コード）。複数の複合クリーンアップ行に埋まっており、除去のリスク＞便益のため温存。
- **リンター/フォーマッタ未導入**：ESLint/Prettier の設定もスクリプトも無い。「完成一歩手前」での新規ツール導入は方針（設計/ツール変更を避ける）に反するため**追加しない**。代わりに `tsc --strict` ＋ 一回限りの `noUnusedLocals` チェックで死蔵コードを掃除済み。導入可否は人間判断（下記）。
- **難易度バランス**：AI はヒューリスティックで調整済み。明らかな破綻は観測されず、仕様変更にあたる調整はしていない（実プレイでの体感確認を推奨）。
- **OGP の `og:image`**：ホスト済み画像アセットが無いため未設定（タイトル/説明の OGP のみ追加）。共有時のサムネ画像が必要なら画像を用意して追加。

---

## 3. 人間の判断が必要なリスト

1. **リンター/フォーマッタの導入可否**：現状なし。導入するなら別途セットアップ作業（依存追加・設定・CI 連携）が必要。今回は方針に従い見送り。
2. **`og:image`（SNS 共有サムネ）**：必要なら 1200×630 程度の画像を `public/` に置き、`index.html` に `og:image`/`twitter:card` を追加。
3. **クロスブラウザ目視**：自動確認は Chromium のみ。Safari / Firefox（特に iOS Safari）でのレイアウト・タッチ操作・`background-clip:text` のタイトル表示・ピンチズームは**実機目視が必要**。
4. **オンライン対戦の本番疎通**：サーバ側の不正メッセージ耐性は修正したが、Render 上での実デプロイ・`ALLOWED_ORIGINS` 設定・2端末同時接続の通し確認は実環境で。
5. **難易度・テンポの体感**：CPU の強さ/速度が「競るが理不尽でない」かは実プレイで最終判断を。

---

## 4. 残タスクと推奨される次の一手

- **(推奨1) `finishing-touches` を `main` へマージ**：6 コミットはいずれも小さく検証済み。特に重大バグ2件（最長交易路の再計算漏れ・サーバDoS）は早期に取り込む価値が高い。
- **(推奨2) 実機/クロスブラウザ目視**：上記「人間の判断」3 を一通り。問題が出た箇所だけ追修正。
- **(完了) 拡張プランの発展項目**：A-5/A-6/B-5/B-6/C-2/C-4/C-7 は本ブランチで実装済み（上記「0.」参照）。残るは下記の任意項目のみ。
- **(任意) C-2 数値カウントアップ**：着地ポップは実装済み。手札数値のカウントアップは再描画モデルと衝突しリスクが高いため見送り（必要なら描画モデルに手を入れる小タスクとして別途）。
- **(任意) `og:image` と PWA 化**：公開を広げるなら共有サムネ＋ manifest/アイコンの整備。

---

### 補足: 検証コマンド

```bash
npm install
npm run build        # tsc + vite build（緑）
npx tsc --noEmit     # 型チェック（緑）
npx vitest run       # 519件（緑）
npm run dev          # ローカル起動（CPU対戦はサーバ不要）
npm start            # オンライン対戦サーバ（別ターミナル）
```
