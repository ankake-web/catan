# Dependencies
node_modules/

# Build output
dist/

# Editor / OS
.DS_Store
*.log

# Claude Code local/plugin settings (machine-specific, not shared)
.claude/

# Compiled test artifacts (sources are TypeScript in tests/)
tests/*.js

# 盗賊などソース画像のスクラッチ置き場（実行時は src/assets/ を使用）
/asset/

# ルート直下のスクラッチ用playwrightスクリプト（検証専用・共有しない）
/screenshot.mjs
/_verify*.mjs

# 地形画像の元データ（リサイズ前の大きいPNG・リポジトリには含めない）
image/
